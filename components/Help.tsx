import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

export default function Help({help}: {help: {icon: string, text: string}}) {
  return (
    <View>
      <Text>Help</Text>
    </View>
  )
}

const styles = StyleSheet.create({})